(function(){
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const value=v=>v==null||v===''?'—':esc(v);
  const validUrl=v=>{try{const u=new URL(String(v).trim());return['http:','https:'].includes(u.protocol)}catch{return false}};
  const flag=l=>RadarEngine.emoji[l]||'🌐';
  const countryMeta=l=>({日语:['JP','JAPAN','东京 · 日本'],韩语:['KR','KOREA','首尔 · 韩国'],德语:['DE','GERMANY','柏林 · 德国'],西班牙语:['ES','SPAIN','马德里 · 西班牙'],意大利语:['IT','ITALY','罗马 · 意大利'],俄语:['RU','RUSSIA','莫斯科 · 俄罗斯'],法语:['FR','FRANCE','巴黎 · 法国']}[l]||['GL','GLOBAL','国际考试']);
  function empty(msg){return`<div class="empty">${esc(msg)}</div>`}
  function section(title,kicker,body,extra=''){return`<section class="section"><div class="section-head"><div><div class="section-kicker">${kicker}</div><h2>${title}</h2></div>${extra}</div>${body}</section>`}
  function focusCard(e){const country=countryMeta(e.language);return`<article class="card focus-card" data-language="${esc(e.language)}" style="--accent:${CalendarEngine.colors[e.eventType]||'#1f6b50'}"><div class="country-mark"><b>${country[0]}</b><span>${country[1]}</span></div><div class="card-top"><span class="lang">${flag(e.language)} ${esc(e.language)}<small>${country[2]}</small></span><span class="badge ${String(e.radarLevel).includes('🔴')?'red':'warn'}">${esc(e.radarLevel||'•')} ${esc(e.marketingStage||'近期节点')}</span></div><h3>${esc(e.examName)}</h3><div class="session">${esc(e.sessionName)} · ${esc(e.session.sessionMonth?e.session.sessionMonth+'月':'')}</div><div class="node-box"><span>${esc(e.eventType)} · ${DateUtils.formatDate(e.eventDate)}</span><strong>${RadarEngine.countdown(e)}</strong></div>${e.advisorActions?`<div class="actions-inline">${RadarEngine.splitActions(e.advisorActions).map(x=>`· ${esc(x)}`).join('<br>')}</div>`:''}<div class="btns"><button class="btn outline" data-detail="${esc(e.recordId)}">考试详情</button><button class="btn primary" data-moments="${esc(e.eventId)}">生成朋友圈</button></div></article>`}
  function todayRows(r){const rows=[...r.todayEvents.map(e=>({icon:'🚨',title:`${e.examName}｜今日${e.eventType}`,sub:e.sessionName,badge:'今日考试节点',recordId:e.recordId})),...r.todayUpdates.map(e=>({icon:'🆕',title:`${e.examName}｜${TemplateEngine.updateText(e)}`,sub:`${e.sessionName}${e.updateNote?' · '+e.updateNote:e.updateType==='首次录入'?' · 该考试场次今日新增至考试日历。':''}`,badge:e.updateType||'信息更新',recordId:e.session.recordId}))];return rows.length?`<div class="event-list">${rows.map(x=>`<div class="event-row"><div class="event-icon">${x.icon}</div><div><h3>${esc(x.title)}</h3><p>${esc(x.sub)}</p></div><button class="badge" data-detail="${esc(x.recordId)}">${esc(x.badge)}</button></div>`).join('')}</div>`:empty('✅ 今天暂无考试当天关键节点')}
  function actionList(actions,today){return actions.length?`<div class="action-board"><div class="action-board-head"><span><i></i> 今日行动清单</span><em>${actions.length} 项</em></div><div class="checklist compact">${actions.map((a,i)=>`<label class="check-item"><input type="checkbox" data-action="${i}" ${localStorage.getItem(`radar-action-${DateUtils.dateKey(today)}-${i}`)==='1'?'checked':''}><span>${esc(a)}</span></label>`).join('')}</div></div>`:empty('今天暂无规则匹配的顾问行动，建议查看未来 7 天营销机会。')}
  function pulseStats(r,data){const todayCount=r.todayEvents.length+r.todayUpdates.length,languageCount=new Set(data.sessions.map(x=>x.language).filter(Boolean)).size,next=r.events.filter(e=>e.daysUntil>=0).sort((a,b)=>a.daysUntil-b.daysUntil)[0];return`<div class="pulse-grid"><article class="pulse-card featured"><div><span class="pulse-label"><i></i> 今日雷达强度</span><strong>${todayCount?r.monthlyFocus.length+todayCount:r.monthlyFocus.length}</strong><p>${todayCount?`今天有 ${todayCount} 条关键动态`:'今天节奏平稳，关注近期机会'}</p></div><div class="pulse-ring" style="--score:${Math.min(92,28+(r.monthlyFocus.length+todayCount)*9)}"><span>${todayCount?'活跃':'平稳'}</span></div></article><article class="pulse-card"><span class="pulse-icon violet">✦</span><div><small>本月营销机会</small><strong>${r.monthlyFocus.length}</strong></div><em>FOCUS</em></article><article class="pulse-card"><span class="pulse-icon blue">↗</span><div><small>未来 7 天节点</small><strong>${r.upcomingEvents.length}</strong></div><em>NEXT 7D</em></article><article class="pulse-card"><span class="pulse-icon cyan">◎</span><div><small>已覆盖语种</small><strong>${languageCount}</strong></div><em>${next?`最近 ${next.daysUntil===0?'今天':next.daysUntil+'天'}`:'全年'}</em></article></div>`}
  function momentsCard(data,e){const copies=TemplateEngine.buildMoments(data,e);if(!copies.length)return'';return`<article class="card moments-card" data-event="${esc(e.eventId)}"><div class="moment-head"><div><span class="lang">${flag(e.language)} ${esc(e.language)}</span><h3>${esc(e.examName)}</h3><p class="muted">${esc(e.sessionName)} · ${esc(e.eventType)} · ${esc(e.marketingStage)}</p></div><span class="dynamic-badge"><i></i> 动态生成</span></div><div class="tabs">${copies.map((c,i)=>`<button class="tab ${i===0?'active':''}" data-copy-tab="${i}">${esc(c.copyType)}</button>`).join('')}</div><div class="moment-content"><h3>${esc(copies[0].renderedTitle)}</h3><div class="copy-box moment-copy">${esc(copies[0].renderedBody)}</div><div class="btns"><button class="btn poster-btn" data-poster>生成朋友圈配图</button><button class="btn primary" data-copy-body="0">复制正文</button><button class="btn outline" data-copy-all="0">复制标题+正文</button></div></div></article>`}
  function radar(data,today){const r=RadarEngine.getTodayRadar(data,today),momentEvents=r.momentsRecommendations.filter(e=>TemplateEngine.buildMoments(data,e,today).length),hasFocus=r.monthlyFocus.length,monthly=hasFocus?r.monthlyFocus:r.events.filter(e=>e.daysUntil>=0).sort((a,b)=>a.daysUntil-b.daysUntil).slice(0,3),monthlyBody=`${!hasFocus?'<div class="warning" style="margin-bottom:14px">本月暂无高优先级考试营销节点，以下是未来最近 3 个节点。</div>':''}${monthly.length?`<div class="grid">${monthly.slice(0,6).map(focusCard).join('')}</div>`:empty('暂无未来考试节点')}`;return`<div class="hero"><div><p class="muted">今天是</p><h2 class="hero-date">${DateUtils.todayLabel(today)}</h2><p class="muted">先看机会，再开始今天的客户沟通。</p></div></div>${pulseStats(r,data)}${section('本月值得关注','MONTHLY FOCUS',monthlyBody)}${section('今天发生什么','TODAY',todayRows(r))}${!r.todayEvents.length&&r.upcomingEvents.length?section('未来 7 天营销机会','UPCOMING',`<div class="event-list">${r.upcomingEvents.map(e=>`<div class="event-row"><div class="event-icon">${flag(e.language)}</div><div><h3>${esc(e.examName)} · ${esc(e.eventType)}</h3><p>${esc(e.sessionName)}，还有 ${e.daysUntil} 天</p></div><span class="badge">${esc(e.marketingStage||'近期')}</span></div>`).join('')}</div>`):''}${section('今天建议你做什么','ACTION LIST',actionList(r.advisorActions,today))}${section('今日朋友圈推荐','MOMENTS',momentEvents.length?`<div class="grid">${momentEvents.map(e=>momentsCard(data,e)).join('')}</div>`:empty('今天暂无匹配的朋友圈文案'))}`}
  function examInfo(b){if(!b)return`<div class="warning">暂未匹配考试基础信息。年度考试场次仍可正常查看。</div>`;const country=countryMeta(b.language),facts=[['考试频率',b.frequency],['通常月份',b.usualMonths],['考试级别',b.levels],['报名费用',b.fee],['出分时间',b.scoreTime],['成绩有效期',b.validity]].filter(x=>x[1]!==''&&x[1]!=null);let route='';if(b.registrationUrl)route=validUrl(b.registrationUrl)?`<a class="btn primary" href="${esc(b.registrationUrl)}" target="_blank" rel="noopener">官方报名入口</a>`:`<p><small class="muted">官方报名方式：</small>${esc(b.registrationUrl)}</p>`;return`<div class="card exam-info country-panel" data-language="${esc(b.language)}"><div><div class="country-title"><span>${flag(b.language)}</span><div><small>${country[0]} · ${country[1]}</small><strong>${esc(b.language)}</strong></div></div><h2>${esc(b.examName)}</h2><p class="muted">${esc(b.intro||'暂无考试简介')}</p>${route}${b.infoSource?`<p class="muted">官方信息来源：${esc(b.infoSource)}</p>`:''}</div><div class="facts">${facts.map(x=>`<div class="fact"><small>${x[0]}</small><strong>${value(x[1])}</strong></div>`).join('')}</div></div>`}
  function timeline(events){const groups=Object.values(events.reduce((a,e)=>{(a[e.recordId]??=[]).push(e);return a},{})).sort((a,b)=>String(a[0].session.examDate).localeCompare(String(b[0].session.examDate)));return groups.length?groups.map(g=>`<div class="timeline-group"><h3>${esc(g[0].examName)} · ${esc(g[0].sessionName)} <span class="badge">${esc(g[0].status||'状态未填写')}</span></h3><div class="timeline">${g.sort((a,b)=>a.eventDate.localeCompare(b.eventDate)).map(e=>`<div class="timeline-item"><i class="timeline-dot" style="--event-color:${CalendarEngine.colors[e.eventType]}"></i><h4>${esc(e.eventType)}</h4><p>${DateUtils.formatDate(e.eventDate,true)}</p></div>`).join('')}</div></div>`).join(''):empty('当前筛选条件下暂无考试节点')}
  function monthCalendar(year,month,events){const monthEvents=events.filter(e=>e.year===year&&e.month===month);return`<section class="month-calendar"><div class="month-title"><div><span>${String(month).padStart(2,'0')}</span><div><p>${year} YEAR CALENDAR</p><h2>${year}年${month}月</h2></div></div><em>${monthEvents.length} 个考试节点</em></div><div class="calendar-head">${'日一二三四五六'.split('').map(x=>`<div>周${x}</div>`).join('')}</div><div class="calendar-grid">${CalendarEngine.naturalMonth(year,month,monthEvents).map(c=>`<div class="cal-day ${c.current?'':'out'}"><div class="cal-number">${c.date.getDate()}</div>${c.events.map(e=>`<div class="cal-event" style="--event-color:${CalendarEngine.colors[e.eventType]}" data-detail="${esc(e.recordId)}">${flag(e.language)} ${esc(e.examName)}<br>${esc(e.eventType)}</div>`).join('')}</div>`).join('')}</div></section>`}
  function miniMonth(year,month,events){const first=new Date(year,month-1,1),days=new Date(year,month,0).getDate(),offset=first.getDay(),cells=Array(offset).fill('<span class="mini-day blank"></span>');for(let day=1;day<=days;day++){const key=DateUtils.dateKey(new Date(year,month-1,day)),hits=events.filter(e=>e.eventDate===key),dots=[...new Set(hits.map(e=>CalendarEngine.colors[e.eventType]))].slice(0,3),preview=hits.length?`<span class="event-preview" aria-hidden="true"><span class="preview-date"><b>${month}月${day}日</b><em>${hits.length} 个考试节点</em></span>${hits.slice(0,4).map(e=>`<span class="preview-event"><i style="--event-color:${CalendarEngine.colors[e.eventType]||'#7379dc'}"></i><span><strong>${flag(e.language)} ${esc(e.examName)}</strong><small>${esc(e.sessionName||'')} · ${esc(e.eventType)}</small></span></span>`).join('')}${hits.length>4?`<span class="preview-more">还有 ${hits.length-4} 个节点，点击查看详情</span>`:''}</span>`:'';cells.push(`<button class="mini-day ${hits.length?'has-event':''}" ${hits.length?`data-detail="${esc(hits[0].recordId)}" aria-label="${esc(`${month}月${day}日，${hits.length}个考试节点`)}"`:''}><span class="day-number">${day}</span>${dots.length?`<i class="event-dots">${dots.map(c=>`<b style="--dot:${c}"></b>`).join('')}</i>`:''}${preview}</button>`)}return`<article class="mini-month"><header><strong>${String(month).padStart(2,'0')}</strong><div><b>${month}月</b><small>${['JAN','FEB','MAR','APR','MAY','JUN','JUL','AUG','SEP','OCT','NOV','DEC'][month-1]}</small></div><em>${events.filter(e=>e.month===month).length} EVENTS</em></header><div class="mini-week">${'日一二三四五六'.split('').map(x=>`<span>${x}</span>`).join('')}</div><div class="mini-grid">${cells.join('')}</div></article>`}
  function annualCalendar(year,events){const yearEvents=events.filter(e=>e.year===year),languages=[...new Set(yearEvents.map(e=>e.language))];return`<section class="annual-shell"><div class="annual-hero"><div><p>SHINYWAY · LANGUAGE EXAM CALENDAR</p><h2>${year}</h2><span>全年小语种考试节点总览</span></div><div class="annual-summary"><strong>${yearEvents.length}</strong><small>个考试节点</small><i></i><strong>${languages.length}</strong><small>个语种</small></div></div><div class="annual-months">${Array.from({length:12},(_,i)=>miniMonth(year,i+1,yearEvents)).join('')}</div><div class="annual-legend">${Object.entries(CalendarEngine.colors).map(([name,color])=>`<span><i style="--dot:${color}"></i>${name}</span>`).join('')}<em>点击带色点的日期查看考试详情</em></div></section>`}
  function calendar(data,state){const all=RadarEngine.buildExamEvents(data.sessions,data.rules,state.today),years=[...new Set(all.map(e=>e.year).concat(data.sessions.map(s=>Number(s.year))))].filter(Boolean).sort(),languages=App.languages(),exams=[...new Set(data.sessions.filter(s=>!state.language||s.language===state.language).map(s=>s.examName))].filter(Boolean).sort(),f={year:+state.year,language:state.language,exam:state.exam,month:+state.month||0,q:'',view:state.view},events=CalendarEngine.filterEvents(all,f),selectedBasic=data.basics.find(b=>(!f.language||b.language===f.language)&&(!f.exam||DataNormalizer.standardName(b.examName)===DataNormalizer.standardName(f.exam))),views=`<div class="calendar-views"><button class="${state.view==='annual'?'active':''}" data-view="annual"><i>▦</i>全年历</button><button class="${state.view==='month'?'active':''}" data-view="month"><i>□</i>月历</button><button class="${state.view==='timeline'?'active':''}" data-view="timeline"><i>≡</i>时间轴</button></div>`,content=state.view==='annual'?annualCalendar(f.year,events):state.view==='month'?monthCalendar(f.year,f.month||new Date().getMonth()+1,events):timeline(events),examCard=state.view!=='annual'&&f.exam?examInfo(selectedBasic):'';return`<div class="calendar-intro"><div><p class="section-kicker">ANNUAL EXAM ATLAS</p><h2>${f.year} 全年考试日历</h2><span>先看全年分布，再按语种、考试或月份深入查询。</span></div>${views}</div><div class="filters calendar-filters"><label><span>年份</span><select data-filter="year">${years.map(y=>`<option ${y==f.year?'selected':''}>${y}</option>`).join('')}</select></label><label><span>语种</span><select data-filter="language"><option value="">全部语种</option>${languages.map(l=>`<option ${l===f.language?'selected':''}>${esc(l)}</option>`).join('')}</select></label><label><span>考试</span><select data-filter="exam"><option value="">全部考试</option>${exams.map(x=>`<option ${x===f.exam?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label><span>月份</span><select data-filter="month"><option value="0">全年</option>${Array.from({length:12},(_,i)=>`<option value="${i+1}" ${i+1===f.month?'selected':''}>${i+1}月</option>`).join('')}</select></label></div>${examCard}${content}`}
  function library(data,state){const selected=state.language||'',languages=App.languages().filter(l=>data.basics.some(b=>b.language===l)),groups={};data.basics.filter(b=>!selected||b.language===selected).forEach(b=>(groups[b.language]??=[]).push(b));const selector=`<div class="language-selector"><div class="language-selector-head"><div><p class="section-kicker">LANGUAGE DIRECTORY</p><h2>选择语种</h2></div><span>${data.basics.length} 项考试</span></div><div class="language-chips"><button class="language-chip ${!selected?'active':''}" data-library-language=""><i>◎</i><span>全部语种</span></button>${languages.map(l=>`<button class="language-chip ${selected===l?'active':''}" data-library-language="${esc(l)}"><i>${flag(l)}</i><span>${esc(l)}</span><small>${data.basics.filter(b=>b.language===l).length}</small></button>`).join('')}</div></div>`;return`${selector}${Object.entries(groups).map(([l,items])=>{const country=countryMeta(l);return`<section class="library-group country-group" data-language="${esc(l)}"><div class="country-group-head"><h2>${flag(l)} ${esc(l)}</h2><span>${country[0]} / ${country[1]}</span></div><div class="grid">${items.map(b=>`<article class="card country-card" data-language="${esc(l)}"><div class="country-ribbon"><i></i><b>${country[0]}</b></div><h3>${esc(b.examName)}</h3><div class="facts"><div class="fact"><small>考试频率</small>${value(b.frequency)}</div><div class="fact"><small>通常月份</small>${value(b.usualMonths)}</div><div class="fact"><small>考试级别</small>${value(b.levels)}</div><div class="fact"><small>成绩有效期</small>${value(b.validity)}</div></div><div class="btns"><button class="btn outline" data-basic="${esc(b.examId)}">查看考试说明</button><button class="btn primary" data-exam-calendar="${esc(b.examName)}" data-exam-id="${esc(b.examId)}" data-lang="${esc(b.language)}">查看全年安排</button></div></article>`).join('')}</div></section>`}).join('')||empty('该语种暂时没有启用的考试')}`}
  function invitation(data,today=new Date()){const items=data.invitations||[],events=RadarEngine.buildExamEvents(data.sessions,data.rules,today),matches=[];events.forEach(e=>items.forEach(item=>{const sameLang=!item.language||item.language==='全部'||item.language===e.language,sameExam=!item.examId&&!item.examName||(item.examId&&item.examId===e.examId)||(item.examName&&DataNormalizer.standardName(item.examName)===DataNormalizer.standardName(e.examName)),sameNode=!item.eventType||item.eventType==='日常'||item.eventType===e.eventType,start=item.startDays===''?-999:Number(item.startDays),end=item.endDays===''?999:Number(item.endDays);if(sameLang&&sameExam&&sameNode&&e.daysUntil>=start&&e.daysUntil<=end)matches.push({...item,event:e})}));const ranked=matches.sort((a,b)=>Number(b.priority||0)-Number(a.priority||0)||Math.abs(a.event.daysUntil)-Math.abs(b.event.daysUntil)).filter((x,i,a)=>a.findIndex(y=>y.contentId===x.contentId)===i),selected=ranked.length?ranked:items.slice().sort((a,b)=>Number(b.priority||0)-Number(a.priority||0)).slice(0,9);const cards=selected.map(x=>{const e=x.event,countdown=e?`对应 ${e.examName} · ${e.eventType}，${RadarEngine.countdown(e)}`:'常备考试内容';return`<article class="invite-card card" data-language="${esc(x.language)}"><div class="invite-top"><span class="invite-type">${esc(x.contentType||'考试谈资')}</span><span class="lang">${flag(x.language)} ${esc(x.language||'全部语种')}</span></div><h3>${esc(x.title||x.examName||'考试邀约内容')}</h3><p class="invite-context">${esc(countdown)}</p><div class="invite-knowledge"><small>备考知识点 / 谈资</small><p>${esc(x.knowledge)}</p></div>${x.inviteScript?`<div class="invite-script"><small>可直接使用的邀约话术</small><p>${esc(x.inviteScript)}</p></div><button class="btn primary" data-copy-invite="${encodeURIComponent(x.inviteScript)}">复制邀约话术</button>`:''}${x.audience?`<p class="invite-audience">适用人群：${esc(x.audience)}</p>`:''}</article>`}).join('');return`<div class="invite-hero"><div><p class="section-kicker">EXAM ENGAGEMENT</p><h2>用专业内容开启一次自然沟通</h2><p>围绕近期考试节点，准备知识点、考试谈资和可直接使用的邀约话术。</p></div><span>${ranked.length?`${ranked.length} 条当前匹配`:'内容库推荐'}</span></div>${section(ranked.length?'当前节点邀约建议':'考试邀约内容库','TODAY ENGAGEMENT',cards?`<div class="invite-grid">${cards}</div>`:empty('暂无考试邀约内容，请在 Excel 的“考试邀约内容”Sheet 中维护。'))}`}
  function reminder(data,today=new Date()){const radar=RadarEngine.getTodayRadar(data,today),wechat=TemplateEngine.buildWechat(data,radar,today);return`<div class="reminder-hero"><div><p class="section-kicker">WECHAT WORK · DAILY BRIEF</p><h2>今日对内营销提醒</h2><p>${DateUtils.todayLabel(today)} · 汇总今日考试节点与信息更新</p></div><span>内部提醒</span></div>${section('今日企微提醒','DAILY SUMMARY',wechat?`<div class="card reminder-card"><div class="copy-box reminder-copy">${esc(wechat)}</div><div class="btns"><button class="btn primary" data-copy-text="${encodeURIComponent(wechat)}">一键复制企微提醒</button></div></div>`:empty('今天暂无需要生成的企微提醒'))}`}
  function welcome(){return`<div class="welcome"><div class="welcome-inner"><div class="welcome-icon">📡</div><h2>新通小语种考试雷达</h2><p class="muted">考试数据尚未加载，请使用线上共享页面或联系数据维护人员。</p></div></div>`}
  function modal(html){document.querySelector('#modalRoot').innerHTML=`<div class="modal-wrap"><div class="modal"><button class="modal-close" data-close>×</button>${html}</div></div>`}
  function detail(s){const source=s.sourceUrl?(validUrl(s.sourceUrl)?`<a class="btn outline" href="${esc(s.sourceUrl)}" target="_blank" rel="noopener">查看本场官方来源</a>`:`<p>官方来源：${esc(s.sourceUrl)}</p>`):'',date=v=>DateUtils.formatDate(v,true)||'—',session=`<section class="session-detail"><p class="section-kicker">EXAM SESSION</p><h2>${esc(s.sessionName)}</h2><p>${flag(s.language)} ${esc(s.examName)} · ${esc(s.region||'地区未填写')}</p>${s.status&&s.status!=='已确认'?`<div class="warning">该考试信息尚未标记为已确认，请以官方最新通知为准。</div>`:''}<div class="facts session-date-facts" style="margin:18px 0"><div class="fact"><small>注册时间</small><strong>${date(s.registrationStart)}</strong></div><div class="fact"><small>报名时间</small><strong>${date(s.applicationStart)}</strong></div><div class="fact"><small>考试日期</small><strong>${date(s.examDate)}</strong></div><div class="fact"><small>适用地区/考点</small><strong>${value(s.region)}</strong></div></div>${s.updateNote?`<p>${esc(s.updateNote)}</p>`:''}${source}</section>`,intro=`<section class="detail-intro"><div class="detail-divider"><span>考试介绍</span></div>${examInfo(s.basic)}</section>`;modal(session+intro)}
  let pageGreeting='';
  function dailyGreeting(){if(pageGreeting)return pageGreeting;const lines=window.InspirationLibrary||['保持热爱，奔赴下一场山海'];pageGreeting=lines[Math.floor(Math.random()*lines.length)]||lines[0];return pageGreeting}
  function radarView(data,today){return radar(data,today).replace('先看机会，再开始今天的客户沟通。',esc(dailyGreeting(today,data)))}
  function calendarView(data,state){return calendar(data,state).replace('<span>先看全年分布，再按语种、考试或月份深入查询。</span>','')}
  window.UI={esc,radar:radarView,calendar:calendarView,library,invitation,reminder,welcome,modal,detail,examInfo,momentsCard,validUrl};
})();
